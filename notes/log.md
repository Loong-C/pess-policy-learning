## 2026-06-04 20:50:26 +08:00

Scope: inspected the repository and Sections 6-7 of `paper/policy_without_overlap.pdf` without source-code edits. The requested log entry is the only intentional text-file change.

Environment and data setup attempt:
- `python setup.py develop` failed immediately because `setup.py` reads a missing `LICENSE` file.
- The default/base Python is not usable for the experiments: importing sklearn fails with a numpy ABI mismatch (`numpy.dtype size changed`).
- Existing conda env `pet-cv` can import numpy/pandas/scipy/sklearn/matplotlib and was used for smoke runs with `PYTHONPATH` set to the repository root.
- Full paper reproduction is not configured: `rpy2`, R/Rscript, R package `policytree`, `openml`, `autograd`, `statsmodels`, and `seaborn` are missing from the checked environments. OpenML real datasets were not downloaded.
- Created `results/` as a runtime output directory. Python imports also generated `__pycache__` directories, and PDF page rendering generated `tmp/`; cleanup was attempted but blocked by sandbox policy.

Run/reproduction check:
- `experiments/MAB_batch.py --setting 1`, `--setting 2`, and `--setting 3` ran successfully under `conda run -n pet-cv` with `PYTHONPATH=<repo root>`, producing `results/results_MAB_*.csv` and `results/freq_MAB_*.csv`.
- MAB settings 1 and 2 emitted numpy warnings about empty-slice means, caused by low-propensity arms sometimes having no observations at small sample sizes.
- `experiments/MAB_batch_clip.py --setting 1` failed with `NameError: abs_mu is not defined` because the script ignores the parsed setting and loops from setting 0.
- Importing `algs.ptree` failed with `ModuleNotFoundError: No module named 'rpy2'`, so all policy-tree based synthetic experiments and the real-data experiment cannot run in the current environment.

Faithfulness and code-quality observations against paper Sections 6-7:
- Algorithm 1 in the paper uses the additive penalty `beta * Bs/T * Gamma_s + beta * Bp/T * Gamma_p`; `algs/pess.py` instead uses a combined `maxV`, an AIPW-dependent linear coefficient, a squared combined variance term, a lower-bound clipping path, and a `Vh` component. This is not a literal implementation of the displayed Algorithm 1.
- The closest CV implementation is `PPL_CV_v3`, but for 5 folds it evaluates only two training prefixes, has off-by-one train/eval overlap, and does not exactly match Algorithm 2's `j = 1, ..., floor(3N/4)` with all later folds as evaluation data.
- `utils/dgp.py` implements `MultiQuad` rewards as `1 - alpha + alpha*x1^2`, while the paper states `1 - alpha/2 + alpha*x1^2/2`.
- `utils/dgp.py` implements `MultiLinear` rewards as `1 - alpha/2 + alpha*x1/2 - alpha*x2`, while the paper Section 7.2.1 states `1 - alpha/2 + x1/2 - x2`.
- `experiments/MAB_batch.py` does not scale the MAB mean vector by `1/sqrt(T)` as stated in Section 7.1.1, and writes raw suboptimality rather than the paper's rescaled suboptimality.
- `experiments/real.py` is not runnable as written: `generate_bandit_data`, `xs_train`, `ps_train`, `DEPTH`, `FLOOR_LIST`, and `floor_id` are undefined; `run_experiment` is called without the required `dgp_model`; and `PL_pessimism` is unpacked as three values though it returns two.
- `experiments/real.py` lists 32 OpenML dataset names, while Section 7.3 says 33 datasets.
- Several experiment arguments are ambiguous or inconsistent: some scripts treat `--beta` as a 1-based index (`BETA_LIST[beta_id-1]`, so default 0 selects the last beta), while `real.py` uses `--beta_id` as 0-based.

Conclusion:
- Partial reproduction is possible only for the simple MAB non-clipping scripts after selecting `pet-cv` and setting `PYTHONPATH`.
- Full reproduction of Sections 6-7 is currently blocked by environment gaps and multiple runtime/code inconsistencies.
- The implementation is related to the paper but cannot be considered a fully faithful, self-contained reproduction of Sections 6-7 in its current state.

## 2026-06-05 20:33:27 +08:00

Scope: after the README-only commit `61a56a0`, continued Step 1 by configuring a complete local environment and downloading the real-data OpenML cache. No algorithm source files were modified.

Git/README:
- Updated `README.md` earlier to state that this repository is an attempted reproduction workspace.
- Committed and pushed that README-only change to `origin/main` as `61a56a0` (`Document reproduction attempt`).
- Confirmed with the user that `paper/` may be committed. `results/` was generated by my MAB smoke runs on 2026-06-04 and is not source material.

Environment:
- Created `pess-pl` with Python 3.11, R 4.4, `rpy2`, `openml`, and R `policytree`/`grf`; external dependencies installed and OpenML access worked, but importing `algs.ptree` failed because `rpy2` 3.6 raises on the deprecated `numpy2ri.activate()` call.
- Created `pess-pl-legacy` from conda-forge with Python 3.10, R 4.1.3, `rpy2=3.4.5`, `r-policytree=1.2.3`, `r-grf=2.3.2`, and the Python scientific dependencies.
- Set local conda env vars for `pess-pl-legacy`: `R_HOME=D:\Users\hp\anaconda3\envs\pess-pl-legacy\Lib\R` and `PYTHONPATH=F:\Personal\Code\pess-policy-learning`.
- Verified `pess-pl-legacy` imports: numpy, pandas, scipy, sklearn, autograd, statsmodels, matplotlib, seaborn, openml, rpy2, `utils.experiment`, `algs.ptree`, and `algs.pess`.
- Verified R packages in `pess-pl-legacy`: `policytree` and `grf`.
- Ran a small policy-tree smoke test through Python -> rpy2 -> R `policytree`; greedy and one-round PPL both completed.

Data:
- Downloaded and cached all 32 OpenML dataset names currently listed in `experiments/real.py` under `data/openml/`.
- Wrote dataset cache manifest to `notes/openml_cache_manifest.csv`.
- Download status: 32/32 successful. Largest cached dataset in the listed set was `Click_prediction_small` with 1,496,391 rows and 9 features.
- OpenML emitted future warnings that `dataset_format="array"` support will be removed in a future OpenML version, which matters for `experiments/real.py`.

Repository hygiene:
- Added `.gitignore` entries for Python caches, `tmp/`, `results/`, and `data/openml/` so generated runtime data is not accidentally committed.
- Added `environment.yml` for the working legacy conda environment.

## 2026-06-05 21:48:18 +08:00

Scope: repaired the code and experiment entry points toward a faithful Section 6-7 reproduction, then ran reproducibility checks and generated result artifacts.

Code repairs:
- Reimplemented `algs/pess.py` around the paper's Algorithm 1 MM update: greedy initialization, `Vs`/`Vp`, additive `Gamma_s` and `Gamma_p` penalties, exact prediction-based convergence, and Algorithm 2 style CV over consecutive folds.
- Updated `algs/ptree.py` to use local rpy2 conversion instead of deprecated global `numpy2ri.activate()`, and fixed logged-data empirical evaluation to score the selected arm.
- Corrected synthetic DGP formulas in `utils/dgp.py` for `MultiQuad` and `MultiLinear` to match Section 7.
- Cleaned `utils/thompson.py`, `utils/experiment.py`, and `utils/compute.py` so TS collection, propensity floors, and experiment outputs are deterministic and explicit.
- Added package `__init__.py` files and fixed `setup.py`/`environment.yml` so editable installation includes `algs`, `utils`, and `experiments`.
- Replaced old broken experiment scripts with compatibility wrappers that delegate to `experiments/reproduce.py`.
- Added `experiments/reproduce.py` as the maintained CLI for MAB, contextual non-adaptive experiments, TS synthetic experiments, TS CV, and real-data experiments.

Paper checks:
- Re-read Section 6 Algorithm 1/2 and Section 7 experiment descriptions.
- Also checked the Section 7.1.2 linear PEVI description and implemented the action-block linear baseline.
- Confirmed the real-data experiment uses 33 OpenML datasets; added the missing `skin-segmentation` dataset.

Environment and data:
- Re-ran `python -m pip install -e .` in `pess-pl-legacy`; editable install succeeded.
- Regenerated `notes/openml_cache_manifest.csv` with 33/33 OpenML datasets successfully cached under `data/openml/`.
- Added `tabulate` for stable Markdown tables.

Runs completed:
- `python experiments/reproduce.py --mode quick --experiment mab --seed 20260605`
- `python experiments/reproduce.py --mode quick --experiment tree --seed 20260605`
- `python experiments/reproduce.py --mode quick --experiment ts --seed 20260605`
- `python experiments/reproduce.py --mode quick --experiment ts-cv --seed 20260605`
- `python experiments/reproduce.py --mode quick --experiment real --seed 20260605`
- `python experiments/reproduce.py --mode full --experiment mab --seed 20260605`

Artifacts:
- Quick all-chain CSVs, figures, and automatic report were written under `artifacts/reproduction/quick/`.
- Full MAB CSVs, figures, and automatic report were written under `artifacts/reproduction/full/`.
- A manually summarized Chinese report was written to `artifacts/reproduction/reproduction_report_zh.md`.

Results and caveats:
- Full MAB matches the paper setup with N=1000. PPL improves over GPL most clearly at small T in the optimal and suboptimal overlap settings; at T=20000 the advantage is small and not statistically significant in this run.
- Quick contextual and TS experiments verify that the full code path runs and show the expected direction: PPL usually improves over GPL, while linear PEVI is more stable in worst-overlap quick setting.
- Quick real-data run uses a deliberately small `cmc` subset and is not statistically conclusive (`PPL - GPL` mean improvement about 0.0058, one-sided p about 0.25).
- Full Figure 5-10 grids were not completed in this interaction. A real-data run using a larger quick subset exceeded 20 minutes and left no CSV before timeout, so quick real was narrowed to a smoke subset. Full mode remains configured for the paper-scale commands, but running it will require substantially more wall-clock time.
- Current OpenML metadata for `houses` has 3842 unique target values (`median_house_value`), which conflicts with the paper's classification-to-bandit assumption. Added a max-arm guard in the real-data transform and a `real_skipped.csv` path for such cases.
